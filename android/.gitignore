/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Signing material is NOT committed: it lives in GitHub Actions secrets
# (ANDROID_KEYSTORE_BASE64 / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD).
key.properties
**/*.keystore
**/*.jks
