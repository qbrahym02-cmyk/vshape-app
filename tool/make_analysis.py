# -*- coding: utf-8 -*-
"""Generates a deep, data-driven analysis of the V-System app + its content plan."""
import json, io, collections

c = json.load(io.open('content.json', encoding='utf-8'))
w, f, wk = c['water'], c['food'], c['workout']
days = sorted(wk['days'], key=lambda d: d['day'])
names = {1: 'الاثنين', 2: 'الثلاثاء', 3: 'الأربعاء', 4: 'الخميس',
         5: 'الجمعة', 6: 'السبت', 7: 'الأحد'}

L = []
A = L.append

A("# 📊 تحليل تطبيق «نظام V» — V-System")
A("")
A("> تحليل هندسي + تحليل للمحتوى (الخطة نفسها)، مبني على قراءة **كل سطر في المستودع**،")
A("> وفحص الروابط والإصدارات وتشغيلات GitHub Actions **فعلياً** — لا من الوصف.")
A("> التاريخ: ١٦ سبتمبر ٢٠٢٦.")
A("")
A("---")
A("")
A("## 1) البطاقة التقنية")
A("")
A("| البند | القيمة |")
A("|---|---|")
A("| الإطار | Flutter (Dart SDK ^3.9) · Flutter 3.47.4 في CI |")
A("| معرّف التطبيق | `com.qbrahym.vsystem` (namespace الكود: `com.qbrahym.vshapeapp`) |")
A("| الحد الأدنى | Android 6.0 (API 23) |")
A("| أدوات أندرويد | Kotlin 2.4.0 · AGP 9.1.0 · Gradle 9.3.1 · Java 17 |")
A("| المكتبات | `dio` · `shared_preferences` · `path_provider` · `url_launcher` — **أربع فقط** |")
A("| الكود | ~5,500 سطر Dart (22 ملف) + ~700 سطر Kotlin (8 ملفات) |")
A("| الاختبارات | 50 اختبار: شاشات (ar/en) · منطق · محتوى · تنبيهات · مؤقّت الراحة · المحدّث |")
A("| التنبيهات | `AlarmManager` مكتوب يدوياً — بدون `flutter_local_notifications` |")
A("| الخط/اللغة | Cairo مدمج · عربي RTL + English · فاتح/داكن/حسب النظام |")
A("| التوقيع | مفتاح release في **أسرار GitHub Actions** (غير مرفوع في المستودع إطلاقاً) |")
A("| البناء | GitHub Actions (٧ جيجا رام) — مساحة العمل المحلية (١ جيجا) تقتل Gradle |")
A("")
A("### لماذا هذه الاختيارات موفّقة")
A("- **٤ مكتبات فقط** ← بناء سريع، سطح أعطال صغير، ولا اعتماد على إضافات قد تُهجر.")
A("- **تنبيهات يدوية بـ `AlarmManager`** ← تدق والتطبيق مغلق، وتعود بعد إعادة التشغيل")
A("  (`BootReceiver`)، وتُعاد جدولتها **بنفس الدقة** يومياً فلا تتأخر تحت وضع Doze.")
A("- **رسوم التمارين بـ `CustomPaint`** ← صفر بايت زيادة (٢٠ صورة كانت ستكلف ١–٢ ميجا).")
A("- **الودجت بـ `RemoteViews`** ← بدون Compose/Glance: لا حجم إضافي ولا وقت بناء أطول.")
A("- **بدون R8/minify** ← مقايضة مقصودة: بناء أخف وأبسط، والتطبيق صغير أصلاً.")
A("")
A("---")
A("")
A("## 2) مسار التحديث — كيف يصل التغيير لجوالك")
A("")
A("```")
A("① تعديل محتوى (بدون APK جديد)          ② إصدار تطبيق جديد")
A("content.json على GitHub                 pubspec.yaml ← git tag v1.0.5+6")
A("      │                                        │")
A("raw.githubusercontent + GitHub Pages    GitHub Actions")
A("      │                                  analyze → tests → حارس المحتوى")
A("التطبيق: «مزامنة الآن» / كل ٦ ساعات       → توقيع → build → Release")
A("      │                                        │")
A("نسخة محلية ← يشتغل أوفلاين              version.json (رقم أساسي بلا إزاحة)")
A("                                               │")
A("                                        التطبيق يقارن versionCode (~كل ٦ ساعات)")
A("                                               │")
A("                                        شريط أخضر → تنزيل → تثبيت فوق النسخة")
A("```")
A("")
A("**ثلاث نقاط تصميم تحمي من كوارث حقيقية:**")
A("1. `assets/content/content.json` (مدمج) مقابل `content.json` (بعيد) — وCI **يفشل** لو")
A("   اختلفا، فلا يمكن نشر APK بمحتوى قديم.")
A("2. `--split-per-abi` يضيف إزاحة لرقم النسخة (arm64 = +2000). `version.json` يحمل الرقم")
A("   **الأساسي**، و`ReleaseInfo.codeIsNormalized` يفرّق بين رقم من الجهاز (مُزاح) ورقم من")
A("   الملف (أساسي). بدون هذا، المحدّث يحسب 4 − 2000 = −1996 فلا يعرض أي تحديث أبداً")
A("   — وهو الخلل الجوهري الذي أُصلح في 1.0.4.")
A("3. التسامح في التحليل: لو جاء JSON ناقص/محترق، `AppContent.fromMap` يعيد قيماً")
A("   افتراضية، والتطبيق يكمل على آخر نسخة سليمة بدل الانهيار.")
A("")
A("---")
A("")
A("## 3) تحليل الخطة (المحتوى نفسه)")
A("")
A("### 💧 الماء")
slot_total = sum(s['glasses'] for s in w['slots']) * w['glass_ml']
A("- الهدف اليومي: **%d مل** (%.2f لتر) · الكوب = %d مل · المدى %d–%d مل"
  % (w['goal_ml'], w['goal_ml'] / 1000, w['glass_ml'], w.get('min_ml', 0), w.get('max_ml', 0)))
A("- المحطات: **%d** = %d كوب = **%d مل** (أي %+d مل عن الهدف)"
  % (len(w['slots']), slot_total // w['glass_ml'], slot_total, slot_total - w['goal_ml']))
A("")
A("| الوقت | المحطة | أكواب | مل | السبب | تنبيه |")
A("|---|---|---|---|---|---|")
for s in w['slots']:
    why = s.get('why', {}).get('ar', '')
    if len(why) > 46:
        why = why[:46] + '…'
    A("| `%s` | %s | %d | %d | %s | %s |"
      % (s['time'], s['title']['ar'], s['glasses'], s['glasses'] * w['glass_ml'], why,
         '✅' if s.get('remind', True) else '—'))
A("")
A("> زيادة المحطات عن الهدف **مقصودة** (هامش أمان للحر/التمرين)، والتطبيق يعتمد")
A("> `max(مجموع المحطات، العدّاد السريع)` فلا يتعارض العدّادان أبداً.")
A("")
A("### 🍽️ الطعام")
tp = sum(m['protein_g'] for m in f['meals'])
tk = sum(m['kcal'] for m in f['meals'])
A("- الهدف: **%d جم بروتين** · %d–%d سعرة" % (f['protein_g_target'], f['kcal_target'][0], f['kcal_target'][1]))
A("- المخطط فعلياً: **%d جم** / **%d سعرة** → هامش %+d جم بروتين فوق الهدف"
  % (tp, tk, tp - f['protein_g_target']))
A("")
A("| الوقت | الوجبة | أصناف | بروتين | سعرات |")
A("|---|---|---|---|---|")
for m in f['meals']:
    A("| `%s` | %s | %d | %d جم | %d |" % (m['time'], m['title']['ar'], len(m['items']), m['protein_g'], m['kcal']))
A("| | **المجموع** | **%d** | **%d جم** | **%d** |" % (sum(len(m['items']) for m in f['meals']), tp, tk))
A("")
A("**دقة العدّ (جديد 1.0.5):** كل صنف صار له `protein_g` و`kcal`، ومجموع أصناف الوجبة =")
A("هدف الوجبة **بالضبط** (يختبره CI). أمثلة: ٤ بيضات = ٢٤ جم/٣٠٠ · كوب فول = ٩ جم/٢٥٠ ·")
A("ربع دجاجة = ٢٤ جم/٢٨٠ · ٢٥٠ جم جبن قريش = ٢٤ جم/٢٥٠ · علبة تونة = ٢٤ جم/٢٨٠.")
A("قبل كان العدد يُقسم بالتساوي على الأصناف، فـ«٤ بيضات» كانت تُحسب مثل «كوب فول».")
A("")
srcs = sorted(f['protein_sources'], key=lambda x: x['rank'])
A("**ملوك البروتين الرخيص (%d مصادر):** " % len(srcs) +
  " · ".join("%d) %s (%s)" % (s['rank'], s['name']['ar'], s['protein']['ar']) for s in srcs[:5]) + " …")
A("")
A("**إضافات سريعة (%d صنف):** " % len(f['extras']['items']) +
  " · ".join("%s %s %s=%dg/%d" % (x['emoji'], x['name']['ar'], '', x['protein_g'], x['kcal'])
             for x in f['extras']['items'][:5]) + " …")
A("")
A("**الممنوعات:** %d بنود · **قاعدة الميزانية:** %s" % (len(f['avoid']), f['budget_rule']['ar'][:90] + '…'))
A("")
A("### 🏋️ التمرين")
tot_sets = sum(e['sets'] for d in days for e in d.get('exercises', []))
A("- **%d أيام**: %d تمرين + %d راحة · **%d حركة** · **%d مجموعة/أسبوع**"
  % (len(days), sum(1 for d in days if not d.get('is_rest')), sum(1 for d in days if d.get('is_rest')),
     sum(len(d.get('exercises', [])) for d in days), tot_sets))
A("- الإيقاع: %s" % wk['tempo_rule']['ar'])
A("- الراحة: %s" % wk['rest_rule']['ar'])
A("")
A("| اليوم | الجلسة | التركيز | حركات | مجموعات |")
A("|---|---|---|---|---|")
for d in days:
    ex = d.get('exercises', [])
    st = sum(e['sets'] for e in ex)
    A("| %s %s | %s | %s | %d | %s |"
      % (names[d['day']], d['emoji'], d['title']['ar'], d['focus']['ar'][:36], len(ex), st or '—'))
A("")
groups = collections.Counter()
for d in days:
    for e in d.get('exercises', []):
        t = e['target']['ar']
        for key, lab in [('صدر', 'صدر'), ('ترايسبس', 'ترايسبس'), ('بايسبس', 'بايسبس'),
                         ('كتف', 'كتف'), ('ظهر', 'ظهر'), ('أفخاذ', 'أرجل'), ('الأرجل', 'أرجل'),
                         ('بطن', 'بطن'), ('أسفل الظهر', 'أسفل الظهر')]:
            if key in t:
                groups[lab] += e['sets']
A("**الحجم الأسبوعي حسب العضلة (مجموعات):**")
A("")
A("| العضلة | مجموعات |")
A("|---|---|")
for k, v in groups.most_common():
    A("| %s | %d |" % (k, v))
A("")
A("> القراءة: الظهر والصدر والأرجل تأخذ الحجم الأكبر، والكتف الجانبي له ٥ مجموعات")
A("> (الرفع الجانبي) لأنه **سر عرض الـ V**. التجديف والبايسبس مكرران مرتين أسبوعياً —")
A("> تردد مناسب لمبتدئ يتدرب بوزن الجسم/الحقيبة، مع يومي راحة يفصلان الحمل.")
A("")
A("### ⏰ الروتين والتنبيهات والقواعد")
A("- **%d بند** روتين من `%s` إلى `%s`" % (len(c['routine']), min(r['time'] for r in c['routine']),
                                          max(r['time'] for r in c['routine'])))
cat = collections.Counter(r['category'] for r in c['routine'])
A("- الفئات: " + " · ".join("%s=%d" % (k, v) for k, v in cat.most_common()))
A("- تنبيهات الماء: %s · التمرين `%s` · النوم `%s`"
  % (' · '.join(c['reminders']['water']), c['reminders']['workout'], c['reminders']['sleep']))
A("- **%d قواعد/تحذيرات** (منها %d عالية الخطورة) · **%d مبادئ** · **%d بنداً** لتجهيز الحقيبة"
  % (len(c['rules']), sum(1 for r in c['rules'] if r.get('severity') == 'high'),
     len(c['philosophy']), len(c['backpack'].get('rules', []))))
A("- مؤشرات التقدم: %d · سجل القوة: %d حركات · القياسات: %d"
  % (len(c['progress']['kpis']), len(c['progress']['strength_log']['exercises']),
     len(c['progress']['measurements'])))
A("")
A("---")
A("")
# ---------------------------------------------------------- v1.1.0 (new) ----
em = c.get('exam_mode', {})
sl = c.get('sleep', {})
ci = c.get('checkin', {})
pt = f.get('price_tool', {})
bl = c['backpack'].get('load', {})
gates = wk.get('safety_gates', [])
mc = c['progress'].get('monthly_checks', [])
rp = c.get('report', {})
priced = [s for s in f.get('protein_sources', []) if s.get('id')]

A("### 🆕 إضافات المحتوى v5 (إصدار التطبيق 1.1.0)")
A("")
A("ستة أقسام جديدة في `content.json`، كلها ثنائية اللغة وقيمها الافتراضية آمنة")
A("(تطبيق قديم يقرأ محتوى جديد لا ينكسر، ومحتوى قديم يخفي الميزة بدل أن يتعطل):")
A("")
if em:
    A("- **وضع الامتحانات** `%s`: يبقي %d جلسات من أصل %d أيام تدريب (%s)."
      % ('exam_mode', em.get('sessions_per_week', 0),
         len([d for d in days if not d.get('is_rest')]),
         ', '.join(em.get('keep_days', []))))
if sl:
    A("- **نوم النمو** `%s`: هدف `%s` وإقفال شاشات `%s` — يُحسب منه سلسلة نوم يومية."
      % ('sleep', sl.get('target'), sl.get('screens_off')))
if ci:
    A("- **تقييم اليوم** `%s`: %d مستويات، عتبة التعب %s، وتخفيف تلقائي بعد %s أيام منخفضة."
      % ('checkin', len(ci.get('levels', [])), ci.get('sore_threshold'), ci.get('deload_after_days')))
if pt:
    A("- **حاسبة سعر البروتين** `food.price_tool`: %d مصدراً صار لها `id` وبروتين لكل وحدة وحد يومي،"
      % len(priced))
    A("  فيُحسب ثمن كل %d جم بروتين وتُبنى أرخص سلة تصل لـ%d جم."
      % (pt.get('per_grams', 20), f.get('protein_g_target', 150)))
if bl:
    A("- **حاسبة وزن الحقيبة** `%s`: قارورة %d مل، رمل %s كجم/لتر، ومدى آمن %s٪ من وزن الجسم."
      % ('backpack.load', bl.get('bottle_ml', 1500), bl.get('sand_kg_per_l'),
         (bl.get('body_pct') or [10, 20])[1]))
if gates:
    A("- **بوابات الأمان** `%s`: %d بوابة (%s) تغطي %s تمريناً، تُقر مرة واحدة."
      % ('workout.safety_gates', len(gates), ', '.join(g['id'] for g in gates),
         sum(len(g.get('exercise_ids', [])) for g in gates)))
if mc:
    A("- **فحوصات الشهر** `%s`: %d مؤشرات تُخزَّن لكل `yyyy-MM` على حدة."
      % ('progress.monthly_checks', len(mc)))
if rp:
    A("- **تقرير الأسبوع** `%s`: ٥ خانات (ماء/بروتين/روتين/تمرين/نوم) بأهداف %s."
      % ('report', ', '.join('%d%%' % round(v * 100) for v in (rp.get('targets') or {}).values())))
A("")
A("المنطق الحسابي كله في `lib/core/plan_math.dart` (بلا أي اعتماد على Flutter) ومختبَر")
A("في `test/plan_math_test.dart`، بينما `lib/core/app_state.dart` يربطه بالتخزين فقط.")
A("")

A("## 4) نقاط قوة حقيقية في التنفيذ")
A("")
A("1. **نماذج متسامحة** — `AppContent.fromMap` يعطي قيمة افتراضية لكل حقل، و`BiList.from`")
A("   يقبل ٣ صيغ مختلفة، فتقدر تعدّل الملف من متصفح الجوال بدون ما تكسر التطبيق.")
A("2. **فصل المحتوى عن الكود** — النصوص/الوجبات/التمارين/الأوقات/التنبيهات كلها في ملف")
A("   واحد بعيد. الكود لا يعرف إلا الشكل العام.")
A("3. **الحالة بمفاتيح مؤرّخة** في `shared_preferences` (`w_2026-09-16`, `sd_<يوم>_<تمرين>_<مجموعة>`)")
A("   ← سجل يومي تلقائي بدون قاعدة بيانات، والـ streak ورسم الأسبوع يقرآن نفس المفاتيح.")
A("4. **`waterTotal() = max(المحطات، السريع)`** — حل أنيق لمشكلة واجهتين لنفس الرقم.")
A("5. **المحدّث يختار APK المطابق للمعالج** ← ٢٢ ميجا بدل ٥٥ ميجا على بيانات الجوال.")
A("6. **CI صارم ومرتّب**: analyze → tests → حارس تزامن المحتوى → بوابة توقيع (يفشل لو ما")
A("   فيه مفتاح بدل نشر نسخة debug) → build universal + split → release + checksums.")
A("7. **رسوم التمارين احتياطية**: أي `id` جديد بدون رسم يظهر بوضعية محايدة — لا ينكسر UI.")
A("")
A("## 5) مخاطر وملاحظات صريحة")
A("")
A("| # | الملاحظة | الأثر | التوصية |")
A("|---|---|---|---|")
A("| 1 | **التوكن الشخصي أُلصق في المحادثة** | صلاحيات كاملة على الحساب (repo/delete_repo/admin:org) | احذفه الآن من settings/tokens وأنشئ غيره |")
A("| 2 | كلمة مرور مفتاح التوقيع كانت نصاً في `BUILD.md` في commit قديم | من يجمعها **مع** ملف jks يوقّع باسمك | الـ jks غير مرفوع ← الخطر منخفض. حُذفت من الوثائق. أمان كامل = تبديل المفتاح (يحتاج إعادة تثبيت مرة واحدة) |")
A("| 3 | `1.0.2`/`1.0.3` فيهما خلل حساب رقم النسخة | لا يظهر لهما شريط التحديث إطلاقاً | ثبّت **1.0.4 أو أحدث** يدوياً مرة واحدة، وبعدها التحديث ذاتي |")
A("| 4 | البناء المحلي مستحيل هنا (١ جيجا رام) | Gradle يُقتل OOM | Actions هو الطريق المضمون |")
A("| 5 | بدون R8/shrink | APK ~٢٢ ميجا بدل ~١٤ | مقبول؛ فعّله لاحقاً لو صار الحجم مشكلة |")
A("| 6 | المستودع **عام** | خطتك وبيانات المحتوى مرئية للجميع | لو تبي خصوصية: Settings → Danger Zone → Change visibility (التحديث عن بُعد يستمر) |")
A("| 7 | الودجت يتحدّث من التطبيق لا من خادم | قد يتأخر رقم لو ما فتحت التطبيق | مقبول لتطبيق أوفلاين؛ التنبيهات اليومية تغطّي الانتباه |")
A("| 8 | مفاتيح التاريخ محلية الجهاز | لو تغيّر منطقه الزمني/التاريخ تتغيّر «اليوم» | طبيعي لتطبيق محلي، ولا يفقد بيانات قديمة |")
A("")
A("---")
A("")
A("## 6) سجل الإصدارات (من GitHub فعلياً)")
A("")
A("| الإصدار | التاريخ (UTC) | المحتوى |")
A("|---|---|---|")
A("| v1.0.2+3 | ١٥ سبتمبر ٠٢:٢٨ | أول نسخة منشورة: التطبيق كامل + تحديث محتوى عن بُعد + تحديث ذاتي للـ APK |")
A("| v1.0.3+4 | ١٥ سبتمبر ١٩:٠٦ | مؤقّت راحة حقيقي (+٣٠ث/إيقاف/تخطي/اهتزاز) · streak صادق · `remind:false` يُحترم · شريط التحديث يرجع كل تشغيل · حذف كلمة المرور من الوثائق · حارس تزامن المحتوى |")
A("| v1.0.4+5 | ١٥ سبتمبر ١٩:١٧ | **إصلاح جوهري في المحدّث**: كان يطرح إزاحة المعالج من رقم `version.json` فينتج رقم سالب ولا يعرض أي تحديث أبداً |")
A("| **v1.0.5+6** | **١٦ سبتمبر** | **رسوم التمارين (٢٠) · عدّاد سعرات/بروتين دقيق لكل صنف · إضافات سريعة (١٢) · رسم ٧ أيام · ودجت الشاشة الرئيسية · محتوى v4** |")
A("")
A("---")
A("")
A("## 7) عدّل خطتك بنفسك من الجوال (بدون كمبيوتر)")
A("")
A("افتح <https://github.com/qbrahym02-cmyk/vshape-app/blob/main/content.json> ← 🖊️ ← عدّل ← ارفع `\"version\"` ← Commit ← في التطبيق «مزامنة الآن».")
A("")
A("| تبغى | تعدّل |")
A("|---|---|")
A("| هدف ماء مختلف | `water.goal_ml` |")
A("| إسكات تنبيه محطة | `\"remind\": false` داخل `water.slots[]` |")
A("| وقت التمرين/النوم | `reminders.workout` / `reminders.sleep` |")
A("| مجموعات/تكرارات | `workout.days[].exercises[].sets` / `.reps` |")
A("| تمرين جديد | انسخ كتلة وغيّر `id` (يظهر بوضعية احتياطية حتى يُرسم) |")
A("| وجبة أو صنف | `food.meals[]` — خلّ مجموع أصناف الوجبة = `protein_g`/`kcal` للوجبة (CI يتحقق) |")
A("| صنف «إضافات سريعة» | `food.extras.items[]` ← `id/emoji/name/protein_g/kcal` |")
A("| قاعدة/تحذير | `rules[]` (`severity: high` تلوّنها أحمر) |")
A("")
A("> **قاعدة ذهبية:** كل نص يقبل `{\"ar\": \"…\", \"en\": \"…\"}`، والنص العادي يشتغل للغتين.")
A("> ولو أخطأت في JSON، التطبيق **لا ينهار** — يكمل على آخر نسخة سليمة.")
A("")
io.open('تحليل-نظام-V.md', 'w', encoding='utf-8').write("\n".join(L) + "\n")
print("written %d lines" % len(L))
