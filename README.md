# نظام V · V-System

تطبيق Flutter (أندرويد) مبني خصيصاً لخطة الـ ٦ أشهر: **قائمة مهام محددة مسبقاً + قسم ماء + قسم أكل + تمارين البيت (حقيبة وطاولة) + سجل تقدم** — وكل المحتوى قابل للتحديث **عن بُعد** بدون إعادة تثبيت التطبيق، والتطبيق نفسه يقدر **يحدّث نفسه** من داخل التطبيق.

---

## 1) التحميل والتركيب

1. خذ ملف `V-System-universal.apk` من صف [Releases](../../releases) (أو من مجلد `release/` لو بنيته بنفسك).
2. انقله للجوال وافتحه → **تثبيت**.
3. أول مرة: Android بيطلب إذن «التثبيت من مصادر غير معروفة» — اقبله.

> لو كان عندك نسخة قديمة موقّعة بمفتاح مختلف، احذفها أول ثم ثبّت الجديدة.

---

## ✨ جديد v1.1.0

| الميزة | التفصيل |
|---|---|
| **تقرير الأسبوع** 📅 | نسبة التزامك الحقيقية في آخر ٧ أيام مقابل الأسبوع اللي قبله، في ٥ خانات (ماء · بروتين · روتين · تمرين · نوم) بهدف لكل خانة، + **أضعف خانة** تركّز عليها، + عدد الجلسات المكتملة والأرقام القياسية. `lib/screens/report_screen.dart` |
| **وضع الامتحانات** 🎓 | مفتاح في الإعدادات يخفض الأسبوع إلى **٣ جلسات** (السبت دفع · الأحد سحب · الاثنين أرجل). الأيام المؤجّلة تبهت في شريط الأسبوع ولا تُحسب ضدك في التقرير، ومعاها ٥ قواعد للأسبوع المخفّف. |
| **نوم النمو** 😴 | تسجّل ساعة نومك بضغطة (٢٢:١٥ / ٢٢:٤٥ / ٢٣:٠٠ / ٢٣:٣٠ / ٠٠:٣٠ أو وقت حر) مقابل هدف **٢٣:٠٠** — مع **سلسلة النوم** 🔥 ودقائق التأخير. النوم بعد منتصف الليل يُحسب متأخراً، والصباح الفاضي ما يكسر السلسلة. |
| **تقييم اليوم** 🌡️ | ٥ مستويات من «🚀 جاهز تماماً» إلى «🤒 مرهق/مريض». التطبيق يقول لك كم مجموعة تحذف اليوم، ويومين منخفضين ورا بعض = اقتراح **Deload** بدل الإصابة. |
| **حاسبة سعر البروتين** 💰 | اكتب سعر كل مصدر مرة واحدة → ترتيب من الأرخص للأغلى حسب ثمن كل **٢٠ جم بروتين**، و**أرخص سلة** تصل لـ١٥٠ جم بحدود يومية واقعية (٦ بيضات، ٢ لتر حليب، ٣٠٠ جم قريش…). رمز العملة قابل للتغيير. |
| **حاسبة وزن الحقيبة** ⚖️ | عدد القوارير + نوع الحشو (ماء/نصف رمل/رمل) → الوزن الكلي، و**المدى الآمن لجسمك** (١٠–٢٠٪ من وزنك)، وحكم: خفيف/مناسب/ثقيل. الرمل ١.٦ كجم/لتر مقابل ١.٠ للماء. |
| **بوابة أمان الطاولة** 🛡️ | قبل أول تجديف طاولة: **٩ خطوات** لتأمين الطاولة ١٠٠٪ + إقرار يُحفظ مرة واحدة، وبطاقة خضراء للمراجعة. تفتح تلقائياً لما تختار يوم السحب. |
| **صورة يوم ١٥ + فحوصات الشهر** 📸 | تذكير الصورة الشهرية بنفس الإضاءة مع checklist من ٦ نقاط، و٤ فحوصات تُعلَّم لكل شهر على حدة (صورة · ملابس · قياسات · أقصى تكرارات). |
| **لحظة رقم قياسي** 🏆 | أي رقم أفضل في سجل القوة يفتح احتفالاً — لأنه الدليل الوحيد على البناء بلا ميزان ولا مقاس. |
| **CI ثابت** 🧪 | `flutter test --concurrency=2` — الاختبارات التي تقرأ الساعة الحقيقية (مؤقّت الراحة) ما عادت تتسابق وتفشل عشوائياً. |

**المحتوى صار v5** — الأقسام الجديدة (`exam_mode`, `sleep`, `checkin`, `food.price_tool`, `backpack.load`,
`workout.safety_gates`, `progress.photo_checkpoint`, `progress.monthly_checks`, `report`) تُقرأ كلها من
`content.json`، فأي تعديل ترفعه على GitHub يصل للجوالات بدون تحديث التطبيق.
القيم الافتراضية آمنة: **محتوى v4 قديم على تطبيق جديد = الميزات تختفي ولا شيء ينكسر** (مختبَر).
كل الحسابات في `lib/core/plan_math.dart` (بلا أي اعتماد على Flutter) ومعها **٤٢ اختباراً جديداً** — المجموع **١٠٤**.

> الإصدار السابق 1.0.5 (رسوم التمارين · العدّ الدقيق · الإضافات السريعة · رسم ٧ أيام · الودجت) في سجل git والوسم `v1.0.5+6`.

---

## 2) الأقسام

| التبويب | الوظيفة |
|---|---|
| **اليوم** | ٣ حلقات (مهام/ماء/بروتين) + روتين الساعة البيولوجية + تمرين اليوم + **تقييم اليوم 🌡️** + **نوم النمو 😴** |
| **الماء** | هدف ٣.٥–٤ لتر، عداد زجاجات، وجدول ٥ محطات (استيقاظ/مدرسة/غداء/تمرين/ليل) |
| **الطعام** | ٥ وجبات بمحتوياتها، عدّاد بروتين تلقائي (١٥٠ جم)، ملوك البروتين الرخيص، **حاسبة سعر البروتين 💰**، قائمة الممنوعات |
| **التمرين** | جدول الأسبوع كامل مع مربعات لكل مجموعة، شرح تنفيذ خطوة بخطوة، قواعد Tempo، **مؤقّت راحة**، **وضع الامتحانات 🎓**، **بوابة أمان الطاولة 🛡️** و**حاسبة وزن الحقيبة ⚖️** |
| **المزيد** | التقدم (سجل قوة + قياسات + رسوم بيانية + **فحوصات الشهر**)، **تقرير الأسبوع 📅**، حاسبة الحقيبة، القواعد والتحذيرات، الإعدادات |

---

## 3) التحديث عن بُعد للمحتوى (بدون إعادة تثبيت) ⭐

كل النصوص والوجبات والتمارين وأهداف الماء وأوقات التنبيهات موجودة في ملف واحد:

```
content.json   (في جذر هذا المستودع)
```

**الخطوات:**

1. افتح <https://github.com/qbrahym02-cmyk/vshape-app/blob/main/content.json>
2. اضغط أيقونة القلم ✏️ (Edit this file).
3. عدّل ما تريد:
   - غيّر عدد مجموعات تمرين، أو أضف تمرين جديد.
   - بدّل وجبة أو أضف صنف.
   - غيّر `goal_ml` للماء أو أوقات التنبيهات في `reminders`.
   - أوقف تنبيه محطة ماء معينة: `"remind": false` في `water.slots` (يُحترم من 1.0.3).
   - **مهم:** ارفع `"version"` في أول الملف (مثلاً من `3` إلى `4`).
4. اضغط **Commit changes**.
5. في التطبيق: **الإعدادات ← مزامنة الآن**. انتهى ✅

التطبيق يزامن تلقائياً كل ٦ ساعات أيضاً، ويعمل **أوفلاين** على آخر نسخة محفوظة. لو صار خطأ في الـ JSON، التطبيق لا ينهار — يكمل على آخر نسخة سليمة.

### تغيير مصدر المحتوى
من **الإعدادات ← رابط ملف المحتوى** تقدر تحط أي رابط JSON:
- GitHub raw (الافتراضي)
- GitHub Pages: `https://qbrahym02-cmyk.github.io/vshape-app/content.json`
- Gist
- Google Drive (رابط المشاركة العادي — التطبيق يحوّله تلقائياً)
- أي سيرفر يرجّع JSON

### بنية `content.json` (ملخص)
```jsonc
{
  "version": 3,                  // ارفعه مع كل تعديل
  "updated_at": "2026-09-15",
  "app":     { "name": {"ar": "...", "en": "..."}, "profile": {...} },
  "water":   { "goal_ml": 3750, "glass_ml": 500, "slots": [ {id,time,glasses,title,why,remind} ] },
  "food":    { "protein_g_target": 150, "meals": [ {id,time,title,protein_g,kcal,items[],tip} ],
               "protein_sources": [...], "avoid": [...] },
  "workout": { "tempo_rule": {...}, "rest_rule": {...},
               "days": [ {id, day(1=Mon..7=Sun), emoji, title, focus, is_rest,
                          exercises:[{id,name,target,sets,reps,rest_s,tempo,steps[],note}],
                          rest_plan:[...] } ] },
  "routine": [ {id,time,emoji,title,detail,category} ],
  "backpack":{ "title": {...}, "rules": [...] },
  "progress":{ "kpis": [...], "strength_log": {"exercises":[...]}, "measurements": [...] },
  "rules":   [ {id,emoji,severity,title,text} ],
  "philosophy": [...],
  "reminders": { "water": ["06:32", ...], "workout": "17:25", "sleep": "22:30", "enabled": true }
  // تنبيهات الماء = محطات water.slots (ماعدا remind:false) + أوقات reminders.water الزائدة
  // الوقت المكرر يُحسب مرة واحدة، والإعدادات تعرض الأوقات التي ستدق فعلاً
}
```
كل نص يقبل الشكل `{"ar": "...", "en": "..."}` — عشان التطبيق ثنائي اللغة.

---

## 4) تحديث التطبيق نفسه (APK جديد) من داخل التطبيق ⭐

1. عدّل الكود وارفع `version:` في `pubspec.yaml` — مثال: `1.0.5+6`
   (الرقم بعد `+` هو **versionCode** وهو اللي يقارنه التطبيق).
2. اكتب وش تغيّر في `RELEASE_NOTES.md` — هذا النص هو اللي يقراه المستخدم داخل التطبيق على بطاقة التحديث.
3. `git commit -am "v1.0.5" && git tag v1.0.5+6 && git push origin main --tags`
4. GitHub Actions يبني APK موقّع وينشره في **Releases** تلقائياً (يشغّل التحليل والاختبارات أولاً).
   تُنشر أربعة ملفات: `universal` + نسخة لكل معالج (`arm64-v8a` ← الأصغر والأنسب لأغلب الجوالات).
5. التطبيق يفحص كل ~٦ ساعات (وكل ~٢٠ ساعة لو فيه تحديث معلّق)، ويظهر شريط أخضر «فيه نسخة جديدة» في **كل تشغيل** حتى تحدّث →
   **تنزيل وتثبيت** → يفتح مثبّت Android مباشرة. **بدون كمبيوتر وبدون حذف بياناتك.**

> أول مرة بيطلب Android إذن «تثبيت تطبيقات غير معروفة» — التطبيق يفتح لك الشاشة بنفسه.

> ⚠️ **لمرة واحدة:** النسخ المثبّتة `1.0.2` و`1.0.3` فيها خلل في حساب رقم النسخة
> (كان يطرح إزاحة المعالج من رقم `version.json`) فلا يظهر لها شريط التحديث أبداً.
> ثبّت **1.0.4** يدوياً من [Releases](../../releases) — وبعدها كل التحديثات تصل من داخل التطبيق.
> الخلل أُصلح في `UpdateService.parseRelease` ومغطى باختبار في `test/reminders_test.dart`.

### البناء اليدوي (بدون Actions)
في GitHub: **Actions ← Build & publish APK ← Run workflow**.
أو على جهازك: انظر `BUILD.md`.

---

## 5) البناء على جهازك

```bash
flutter pub get
flutter build apk --release
# الناتج: build/app/outputs/flutter-apk/app-release.apk
```

**المتطلبات:** Flutter 3.47+ · JDK 17 · Android SDK 35 · ذاكرة 4 جيجا أو أكثر.
`local.properties` يُنشأ تلقائياً، أو أنشئه يدوياً:
```
sdk.dir=/path/to/android-sdk
flutter.sdk=/path/to/flutter
```

### التوقيع 🔑
مفتاح الإصدار **غير مرفوع في المستودع إطلاقاً** (تحقّق: `android/keystore/` و`android/key.properties` في `.gitignore`، وما دخلوا git في أي commit سابق). هو محفوظ في مكانين:

1. **Secrets في GitHub Actions** — وهذا ما يوقّع به CI:
   `ANDROID_KEYSTORE_BASE64` (ملف jks بترميز base64) · `KEYSTORE_PASSWORD` · `KEY_ALIAS` · `KEY_PASSWORD`
2. **نسخة احتياطية عندك** — احفظها خارج المستودع (فلاشة/مدير كلمات مرور).

> ⚠️ **لو ضاع المفتاح، ما تقدر تحدّث التطبيق فوق النسخة المثبّتة** — المستخدم سيضطر للحذف وإعادة التثبيت (وتضيع بياناته).
> workflow البناء **يفشل عمداً** إذا ما لقى مادة توقيع، حتى لا يُنشر APK بمفتاح debug بدون ما تنتبه.

للبناء محلياً: أنشئ `android/key.properties` بنفس الحقول وضع ملف `.jks` في `android/keystore/`.

---

## 6) بنية المشروع

```
lib/
  main.dart                     الإقلاع + المزامنة الخلفية
  app.dart                      MaterialApp + الثيم + الاتجاه RTL
  core/
    models.dart                 نماذج محتوى content.json (تتحمل الحقول الناقصة)
    app_state.dart              الحالة + التخزين المحلي (shared_preferences)
    plan_math.dart              منطق الأرقام: الأسعار، الحقيبة، النوم، تقرير الأسبوع (بلا Flutter)
    native_bridge.dart          جسر Kotlin: التنبيهات، التثبيت، معلومات النسخة
    l10n.dart                   نصوص الواجهة (عربي/إنجليزي)
    theme.dart                  الألوان والخطوط
  services/
    content_service.dart        جلب المحتوى + قراءة إصدارات GitHub
    update_controller.dart      منطق فحص/تنزيل/تثبيت التحديث
  screens/                      اليوم · الماء · الطعام · التمرين · المزيد · التقدم · تقرير الأسبوع · القواعد · الإعدادات
  widgets/                      بطاقات وحلقات تقدم ورسوم بيانية · بطاقات اليوم · أداة السعر · حاسبة الحقيبة · بوابات الأمان
android/app/src/main/kotlin/    MainActivity · Alarms · AlarmReceiver · BootReceiver
assets/content/content.json     النسخة المدمجة (تعمل بدون إنترنت)
```

### مؤقّت الراحة ⏱️
اضغط على شريحة «٩٠ ث راحة» في أي تمرين → عدّاد دائري فوق شريط التنقّل، يشتغل بين التبويبات.
`+30` / إيقاف مؤقت / تخطي، وعند النهاية اهتزاز + صوت + تنبيه. المدة تُقرأ من `rest_s` لكل تمرين في `content.json`.

**لا توجد إضافات ثقيلة:** `dio` · `shared_preferences` · `path_provider` · `url_launcher` فقط.
التنبيهات اليومية مكتوبة يدوياً بـ `AlarmManager` (بدون `flutter_local_notifications`) حتى يبقى البناء خفيفاً.

---

## 7) ملاحظة طبية سريعة
الخطة مبنية لشاب عمره ١٥ وطوله ١٨٥–١٩٠ ووزنه ٨٠–٨٥ كغ. أي ألم حاد في المفصل أو الظهر = توقف فوراً.
التقدم الحقيقي يبدأ بصرياً من الشهر الثالث، والنوم قبل ١١ مساءً جزء من التمرين وليس رفاهية.
